#!/bin/bash
# One-Click Launcher for Enigma Purchase Optimizer
# Double-click this file to start the system.

# 1. Navigate to the folder where this file is located
cd "$(dirname "$0")"

# 2. Make sure start.sh is executable
chmod +x start.sh

# 3. clear terminal for neatness
clear

# 4. Run the start script
echo "🟢 Inicializando Enigma Purchase Optimizer..."
./start.sh
