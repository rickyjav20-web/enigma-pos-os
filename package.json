{
  "name": "enigma-pos-os",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:deploy": "npm run db:deploy -w apps/api",
    "db:seed:prod": "npm run db:seed:prod -w apps/api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/node-fetch": "^2.6.13",
    "axios": "^1.13.4",
    "node-fetch": "^3.3.2"
  }
}
